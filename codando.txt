Em src/components/Home.jsx, ajustar o botão "trocar" do header pra só aparecer quando o rep tem mais de 1 empresa.
Localizar (perto da linha 268-279):
jsx<span className="home-rep-nome">{representadaSelecionada?.nome || 'Selecionar'}</span>
<span className="home-rep-trocar">trocar ›</span>
O elemento "trocar" provavelmente está dentro de um botão clicável (<button> ou <div onClick={...}> que abre o sheet). Localizar esse wrapper e:

Se tiver apenas 1 empresa em representadas, não envolver com onClick (ou desabilitar o botão)
Esconder o <span className="home-rep-trocar">trocar ›</span> quando representadas.length <= 1

Algo tipo:
jsx<div 
  className="home-rep-info"
  onClick={representadas.length > 1 ? () => setMostrarSheet(true) : undefined}
  style={{ cursor: representadas.length > 1 ? 'pointer' : 'default' }}
>
  ...
  <span className="home-rep-nome">{representadaSelecionada?.nome || 'Selecionar'}</span>
  {representadas.length > 1 && (
    <span className="home-rep-trocar">trocar ›</span>
  )}
</div>
Adapta pro código real. Mostra o trecho final pra eu validar.